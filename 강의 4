#define _CRT_SECURE_NO_WARINGS

#include<stdio.h>

int main(void)
{
	double rate;
	double usd;
	int krw;

	preintf("환율을 입력하시오:");
	scanf("%d", &rate);

	preintf("원화 금액을 입력하시오:");
	scanf("%d", &krw);

	usd = krw / rate;

	printf("원화 %d뭔은 %if달러입니다.\n", krw, usd);

	return 0;
}
